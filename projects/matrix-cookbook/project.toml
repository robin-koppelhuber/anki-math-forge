title = "The Matrix Cookbook"
citation = "Matrix Cookbook"
deck = "Mathe::Matrix-Calculus"
order = "printed"
tags = []

# The work this project reads: its files, and how to read them. One table per
# work, because a project may hold a tight cluster of papers and a marking
# scheme is a fact about a document rather than about the deck.
[[sources]]
files = ["projects/matrix-cookbook/matrixcookbook.pdf"]

# What is ambient in this book, as one-liners. Free-form: every key reaches
# whoever writes a card, through `forge context`. Prose that wants a paragraph
# belongs in conventions.md. `layout` is the only key anything acts on, in
# `verify`.
[conventions]
layout = "denominator"
entries = "real unless the card says otherwise"
