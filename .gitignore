__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
*.egg-info/
vendor/
node_modules/

# crop working files (--out dirs); the ledger keeps geometry, not pictures
.crops-*/
.tx-*/
# the configured work_dir: crops, page renders, scratch LaTeX
.forge/

# local secrets for MCP servers; .env.example is the template
.env
